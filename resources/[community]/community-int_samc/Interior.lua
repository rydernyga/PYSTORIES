local wejscie = createMarker(-2653.39, 639.15, 14.45+1, "arrow", 1.5, 255, 255, 0, 190)
local wyjscie = createMarker(3954.55, -1184.71, 8.37+1, "arrow", 1.5, 255, 255, 0, 190)
--createBlip(-2653.39, 639.15, 14.45, 22,2,0,0,0,0,0,275)
setElementDimension(wyjscie,0)
setElementInterior(wyjscie,1)

addEventHandler("onMarkerHit", wejscie, function(el,md)
	if isPedInVehicle(el) then return end
	fadeCamera(el ,false)
	setElementFrozen(el, true)
	setTimer(function ()
	outputChatBox( "* Wszedłeś(aś) do Szpitala.", el)
	setElementInterior(el,1)
	setElementDimension(el,0)
	setElementFrozen(el,false)
	setElementPosition(el,3954.54, -1181.25, 8.37)
	setElementFrozen(el, false)
	showPlayerHudComponent(el, "radar", false)
	fadeCamera(el, true)
	end, 1500, 1)
end)

addEventHandler("onMarkerHit", wyjscie, function(el,md)
	if isPedInVehicle(el) then return end
	fadeCamera(el ,false)
	setElementFrozen(el, true)
	setTimer(function ()
	outputChatBox( "* Wyszedłeś(aś) ze Szpitala.", el)
    setElementDimension(el, 0)
    setElementInterior(el, 0)
	setElementFrozen(el,false)
	setElementPosition(el,-2654.86, 634.63, 14.45)
	setElementFrozen(el, false)
	showPlayerHudComponent(el, "radar", true)
	fadeCamera(el, true)
	end, 1500, 1)
end)

--[[createBlip(-2431.41, -114.93, 35.32, 36,2,0,0,0,0,0,275)

wejscie_marker = createMarker (-2431.41, -114.93, 35.32+1, "arrow", 1.5, 255, 200, 0, 150)
wyjscie_marker = createMarker (-2026.88, -103.60, 1035.18+1, "arrow", 1.5, 255, 200, 0, 150)
setElementDimension(wyjscie_marker, 1)
setElementInterior(wyjscie_marker, 3)

addEventHandler("onMarkerHit", wejscie_marker, function(el, md)
    if isPedInVehicle(el) then return end
    fadeCamera(el ,false)
	setElementFrozen(el, true)
	setTimer(function ()
	outputChatBox( "* Wszedłeś(aś) do szkoły jazdy.", el)
    setElementDimension(el, 1)
    setElementInterior(el, 3)
    setElementPosition(el, -2029.72, -106.90, 1035.17)
	setElementFrozen(el, false)
	showPlayerHudComponent(el, "radar", false)
	fadeCamera(el, true)
	end, 1500, 1)
end)

addEventHandler("onMarkerHit", wyjscie_marker, function(el, md)
    if isPedInVehicle(el) then return end
    fadeCamera(el ,false)
	setElementFrozen(el, true)
	setTimer(function ()
	outputChatBox("* Wyszedłeś(aś) ze szkoły jazdy.", el)
    setElementDimension(el, 0)
    setElementInterior(el, 0)
    setElementPosition(el, -2427.54, -114.84, 35.32)
	setElementFrozen(el, false)
	showPlayerHudComponent(el, "radar", true)
	fadeCamera(el, true)
	end, 1500, 1)
end)--]]